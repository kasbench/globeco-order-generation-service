apiVersion: v1
kind: PersistentVolume
metadata:
  name: order-generation-service-mongodb-pv
  labels:
    app: order-generation-service-mongodb
    type: local
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /mnt/disk1/mongodb-order-generation-service
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - node-5
